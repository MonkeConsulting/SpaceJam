name: example
description: Example of the SpaceJam package.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.0-159.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  spacejam:
    path: ../

dev_dependencies:
  all_lint_rules_community: ^0.0.7
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg